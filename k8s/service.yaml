apiVersion: v1
kind: Service
metadata:
  name: reverse-proxy-service
  labels:
    app: reverse-proxy
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: metrics
      port: 9090
      targetPort: 9090
  selector:
    app: reverse-proxy
